import { Button } from './button'

export default {
  component: Button,
  title: 'Button',
}

export const Default = {
  args: {
    variant: 'default',
    size: 'default',
    children: 'Default',
  },
}
